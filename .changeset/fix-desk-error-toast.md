---
'@sheet/web': patch
---

Replace the desktop runtime-error overlay's full-width red monospace banner (which read like a crash/dev artifact across the grid) with a compact, dismissible notice; full error detail goes to the console.
